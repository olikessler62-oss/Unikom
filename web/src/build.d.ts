/**
 * Replaced at build time by `define` in vite.config.ts with the moment the
 * build was made. It is a compile-time constant, not a variable — there is
 * nothing to import and nothing that could be undefined at runtime.
 */
declare const __UNIKOM_BUILD__: string;
